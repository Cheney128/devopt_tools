# 延迟检测实时更新 - WebSocket实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 使用 WebSocket 实现延迟检测数据的实时推送，解决前端延迟数据不更新的问题。

**Architecture:** 后端使用 FastAPI 内置 WebSocket 支持，创建独立的 WebSocket 路由端点。延迟检测服务完成检测后，通过 WebSocket 连接池向所有连接的客户端推送更新。前端使用原生 WebSocket API 建立连接，监听消息并更新设备列表中的延迟数据。

**Tech Stack:** 
- 后端: FastAPI WebSocket, asyncio, ConnectionManager
- 前端: Native WebSocket API, Vue 3 Composition API

---

## 问题背景

### 问题描述
设备管理页面中的延迟检测功能不会实时更新。后端定时任务正常执行延迟检测并更新数据库，但前端页面不会自动刷新显示最新数据。

### 根因分析
```
后端定时任务 → 更新数据库 → ❌ 前端没有轮询/WebSocket → 用户看不到更新
```

前端只在页面加载时获取一次设备数据，之后没有任何机制获取最新的延迟数据。

---

## 模块设计

### 模块架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         后端 (FastAPI)                           │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐    ┌─────────────────┐    ┌──────────────┐ │
│  │ LatencyScheduler │───▶│ LatencyService  │───▶│  Database    │ │
│  │   (定时任务)      │    │   (检测执行)     │    │  (数据存储)   │ │
│  └─────────────────┘    └────────┬────────┘    └──────────────┘ │
│                                  │                               │
│                                  ▼                               │
│                         ┌─────────────────┐                      │
│                         │ ConnectionManager│◀─────┐              │
│                         │  (WebSocket管理)  │      │              │
│                         └────────┬────────┘      │              │
│                                  │               │              │
└──────────────────────────────────┼───────────────┼──────────────┘
                                   │               │
                              WebSocket          广播推送
                                   │               │
┌──────────────────────────────────┼───────────────┼──────────────┐
│                         前端 (Vue 3)            │               │
├──────────────────────────────────┼───────────────┼──────────────┤
│  ┌─────────────────┐             │               │              │
│  │  WebSocketStore │◀────────────┘               │              │
│  │  (连接管理)      │                             │              │
│  └────────┬────────┘                             │              │
│           │                                      │              │
│           ▼                                      │              │
│  ┌─────────────────┐                             │              │
│  │ DeviceManagement│◀────────────────────────────┘              │
│  │    (页面组件)    │                                            │
│  └─────────────────┘                                            │
└─────────────────────────────────────────────────────────────────┘
```

### 模块清单

| 模块 | 文件路径 | 职责 |
|------|----------|------|
| WebSocket连接管理器 | `app/websocket/manager.py` | 管理WebSocket连接、广播消息 |
| WebSocket路由端点 | `app/api/endpoints/websocket.py` | 处理WebSocket连接请求 |
| 延迟检测钩子 | `app/services/latency_service.py` | 检测完成后触发WebSocket推送 |
| 前端WebSocket Store | `frontend/src/stores/websocketStore.js` | 管理WebSocket连接状态 |
| 前端设备页面更新 | `frontend/src/views/DeviceManagement.vue` | 监听WebSocket消息更新UI |

---

## 详细任务分解

### Task 1: 创建WebSocket连接管理器

**Files:**
- Create: `app/websocket/__init__.py`
- Create: `app/websocket/manager.py`

**Step 1: 创建模块目录和初始化文件**

```python
# app/websocket/__init__.py
from app.websocket.manager import ConnectionManager, manager

__all__ = ["ConnectionManager", "manager"]
```

**Step 2: 创建连接管理器**

```python
# app/websocket/manager.py
"""
WebSocket连接管理器
管理所有活跃的WebSocket连接，支持广播消息
"""
from fastapi import WebSocket
from typing import List, Dict, Any
import json
import logging
import asyncio

logger = logging.getLogger(__name__)


class ConnectionManager:
    """
    WebSocket连接管理器
    
    管理所有活跃的WebSocket连接，支持：
    - 连接注册/注销
    - 广播消息到所有连接
    - 发送消息到特定连接
    """
    
    def __init__(self):
        self.active_connections: List[WebSocket] = []
        self._lock = asyncio.Lock()
    
    async def connect(self, websocket: WebSocket):
        """
        接受新的WebSocket连接
        
        Args:
            websocket: WebSocket连接对象
        """
        await websocket.accept()
        async with self._lock:
            self.active_connections.append(websocket)
        logger.info(f"WebSocket connected. Total connections: {len(self.active_connections)}")
    
    async def disconnect(self, websocket: WebSocket):
        """
        断开WebSocket连接
        
        Args:
            websocket: 要断开的WebSocket连接
        """
        async with self._lock:
            if websocket in self.active_connections:
                self.active_connections.remove(websocket)
        logger.info(f"WebSocket disconnected. Total connections: {len(self.active_connections)}")
    
    async def send_personal_message(self, message: Dict[str, Any], websocket: WebSocket):
        """
        发送消息到特定连接
        
        Args:
            message: 消息内容（字典）
            websocket: 目标WebSocket连接
        """
        try:
            await websocket.send_json(message)
        except Exception as e:
            logger.error(f"Failed to send personal message: {e}")
    
    async def broadcast(self, message: Dict[str, Any]):
        """
        广播消息到所有连接
        
        Args:
            message: 消息内容（字典）
        """
        if not self.active_connections:
            return
        
        disconnected = []
        
        for connection in self.active_connections:
            try:
                await connection.send_json(message)
            except Exception as e:
                logger.error(f"Failed to broadcast to connection: {e}")
                disconnected.append(connection)
        
        # 清理断开的连接
        for connection in disconnected:
            await self.disconnect(connection)
    
    async def broadcast_latency_update(self, device_id: int, latency: int, 
                                        last_check: str, status: str):
        """
        广播延迟更新消息
        
        Args:
            device_id: 设备ID
            latency: 延迟值（毫秒）
            last_check: 最后检测时间（ISO格式）
            status: 设备状态
        """
        message = {
            "type": "latency_update",
            "data": {
                "device_id": device_id,
                "latency": latency,
                "last_latency_check": last_check,
                "status": status
            },
            "timestamp": datetime.now().isoformat()
        }
        await self.broadcast(message)
    
    def get_connection_count(self) -> int:
        """
        获取当前连接数
        
        Returns:
            活跃连接数
        """
        return len(self.active_connections)


# 全局连接管理器实例
manager = ConnectionManager()
```

**Step 3: 添加必要的导入**

```python
# 在文件顶部添加
from datetime import datetime
```

---

### Task 2: 创建WebSocket路由端点

**Files:**
- Create: `app/api/endpoints/websocket.py`
- Modify: `app/api/__init__.py`

**Step 1: 创建WebSocket端点**

```python
# app/api/endpoints/websocket.py
"""
WebSocket API端点
提供实时通信能力
"""
from fastapi import APIRouter, WebSocket, WebSocketDisconnect, Depends
from app.websocket import manager
from app.core.security import verify_token
import logging
import json

logger = logging.getLogger(__name__)

router = APIRouter(tags=["websocket"])


@router.websocket("/ws/latency")
async def websocket_latency_endpoint(websocket: WebSocket):
    """
    延迟检测实时更新WebSocket端点
    
    连接后，客户端将实时接收延迟检测更新消息
    
    消息格式:
    {
        "type": "latency_update",
        "data": {
            "device_id": 1,
            "latency": 25,
            "last_latency_check": "2026-03-16T10:30:00",
            "status": "active"
        },
        "timestamp": "2026-03-16T10:30:00.123456"
    }
    """
    await manager.connect(websocket)
    
    try:
        # 发送连接成功消息
        await manager.send_personal_message(
            {"type": "connected", "message": "WebSocket connected successfully"},
            websocket
        )
        
        # 保持连接，监听客户端消息（心跳等）
        while True:
            data = await websocket.receive_text()
            
            try:
                message = json.loads(data)
                
                # 处理心跳消息
                if message.get("type") == "ping":
                    await manager.send_personal_message(
                        {"type": "pong", "timestamp": datetime.now().isoformat()},
                        websocket
                    )
                    
            except json.JSONDecodeError:
                logger.warning(f"Invalid JSON received: {data}")
                
    except WebSocketDisconnect:
        await manager.disconnect(websocket)
        logger.info("Client disconnected")
    except Exception as e:
        logger.error(f"WebSocket error: {e}")
        await manager.disconnect(websocket)


@router.get("/ws/status")
async def get_websocket_status():
    """
    获取WebSocket服务状态
    
    Returns:
        当前连接数等信息
    """
    return {
        "active_connections": manager.get_connection_count(),
        "status": "running"
    }
```

**Step 2: 注册WebSocket路由到API**

```python
# app/api/__init__.py 中添加
from app.api.endpoints.websocket import router as websocket_router

# 在 api_router 中添加
api_router.include_router(websocket_router, prefix="/ws", tags=["websocket"])
```

---

### Task 3: 修改延迟检测服务，添加WebSocket推送

**Files:**
- Modify: `app/services/latency_service.py`

**Step 1: 在延迟检测完成后触发WebSocket广播**

在 `_update_device_latency` 方法中添加WebSocket推送：

```python
# app/services/latency_service.py

# 在文件顶部添加导入
from app.websocket import manager

# 修改 _update_device_latency 方法
def _update_device_latency(
    self, 
    db: Session, 
    device: Device, 
    latency: int, 
    success: bool
):
    """
    更新设备延迟和状态，并广播WebSocket消息
    """
    device.latency = latency
    device.last_latency_check = datetime.now()
    
    if not success:
        if device.status != "maintenance":
            device.status = "offline"
    
    db.commit()
    db.refresh(device)
    
    # 广播延迟更新（异步执行）
    import asyncio
    try:
        loop = asyncio.get_event_loop()
        if loop.is_running():
            asyncio.create_task(
                manager.broadcast_latency_update(
                    device_id=device.id,
                    latency=latency,
                    last_check=device.last_latency_check.isoformat(),
                    status=device.status
                )
            )
    except RuntimeError:
        # 如果没有事件循环，创建一个新的
        asyncio.run(
            manager.broadcast_latency_update(
                device_id=device.id,
                latency=latency,
                last_check=device.last_latency_check.isoformat(),
                status=device.status
            )
        )
```

---

### Task 4: 创建前端WebSocket Store

**Files:**
- Create: `frontend/src/stores/websocketStore.js`

**Step 1: 创建WebSocket Store**

```javascript
// frontend/src/stores/websocketStore.js
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useWebSocketStore = defineStore('websocket', () => {
  // State
  const socket = ref(null)
  const isConnected = ref(false)
  const lastMessage = ref(null)
  const reconnectAttempts = ref(0)
  const maxReconnectAttempts = 5
  const reconnectDelay = 3000 // 3秒
  
  // 心跳相关
  const heartbeatInterval = ref(null)
  const heartbeatTimeout = 30000 // 30秒
  
  // Getters
  const connectionStatus = computed(() => {
    if (isConnected.value) return 'connected'
    if (reconnectAttempts.value >= maxReconnectAttempts) return 'failed'
    return 'disconnected'
  })
  
  // Actions
  
  /**
   * 获取WebSocket URL
   */
  function getWebSocketUrl() {
    const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:'
    const host = window.location.host
    return `${protocol}//${host}/api/v1/ws/latency`
  }
  
  /**
   * 连接WebSocket
   * @param {Function} onMessage - 消息回调函数
   */
  function connect(onMessage) {
    if (socket.value && isConnected.value) {
      console.log('[WebSocket] Already connected')
      return
    }
    
    const url = getWebSocketUrl()
    console.log('[WebSocket] Connecting to:', url)
    
    socket.value = new WebSocket(url)
    
    socket.value.onopen = () => {
      console.log('[WebSocket] Connected')
      isConnected.value = true
      reconnectAttempts.value = 0
      startHeartbeat()
    }
    
    socket.value.onmessage = (event) => {
      try {
        const message = JSON.parse(event.data)
        lastMessage.value = message
        
        // 处理不同类型的消息
        if (message.type === 'latency_update' && onMessage) {
          onMessage(message.data)
        } else if (message.type === 'pong') {
          // 心跳响应
          console.log('[WebSocket] Heartbeat OK')
        } else if (message.type === 'connected') {
          console.log('[WebSocket] Server confirmed connection')
        }
      } catch (error) {
        console.error('[WebSocket] Failed to parse message:', error)
      }
    }
    
    socket.value.onclose = (event) => {
      console.log('[WebSocket] Disconnected:', event.code, event.reason)
      isConnected.value = false
      stopHeartbeat()
      
      // 尝试重连
      if (reconnectAttempts.value < maxReconnectAttempts) {
        reconnectAttempts.value++
        console.log(`[WebSocket] Reconnecting... Attempt ${reconnectAttempts.value}/${maxReconnectAttempts}`)
        setTimeout(() => connect(onMessage), reconnectDelay)
      }
    }
    
    socket.value.onerror = (error) => {
      console.error('[WebSocket] Error:', error)
    }
  }
  
  /**
   * 断开WebSocket连接
   */
  function disconnect() {
    stopHeartbeat()
    if (socket.value) {
      socket.value.close()
      socket.value = null
    }
    isConnected.value = false
    reconnectAttempts.value = 0
    console.log('[WebSocket] Disconnected manually')
  }
  
  /**
   * 发送消息
   * @param {Object} message - 消息对象
   */
  function send(message) {
    if (socket.value && isConnected.value) {
      socket.value.send(JSON.stringify(message))
    }
  }
  
  /**
   * 启动心跳
   */
  function startHeartbeat() {
    stopHeartbeat()
    heartbeatInterval.value = setInterval(() => {
      if (isConnected.value) {
        send({ type: 'ping' })
      }
    }, heartbeatTimeout)
  }
  
  /**
   * 停止心跳
   */
  function stopHeartbeat() {
    if (heartbeatInterval.value) {
      clearInterval(heartbeatInterval.value)
      heartbeatInterval.value = null
    }
  }
  
  return {
    socket,
    isConnected,
    lastMessage,
    reconnectAttempts,
    connectionStatus,
    connect,
    disconnect,
    send
  }
})
```

---

### Task 5: 修改前端设备管理页面，集成WebSocket

**Files:**
- Modify: `frontend/src/views/DeviceManagement.vue`

**Step 1: 导入WebSocket Store**

```javascript
// 在 <script setup> 顶部添加
import { useWebSocketStore } from '../stores/websocketStore'

// 创建store实例
const websocketStore = useWebSocketStore()
```

**Step 2: 添加WebSocket连接逻辑**

```javascript
// 在 onMounted 中添加WebSocket连接
onMounted(() => {
  fetchDevices()
  loadCommandHistory()
  
  // 连接WebSocket
  websocketStore.connect(handleLatencyUpdate)
})

// 添加 onUnmounted 清理
import { onUnmounted } from 'vue'

onUnmounted(() => {
  websocketStore.disconnect()
})
```

**Step 3: 添加延迟更新处理函数**

```javascript
/**
 * 处理WebSocket延迟更新消息
 * @param {Object} data - 延迟更新数据
 */
const handleLatencyUpdate = (data) => {
  console.log('[DeviceManagement] Received latency update:', data)
  
  // 更新设备列表中对应设备的数据
  const deviceIndex = deviceStore.devices.findIndex(d => d.id === data.device_id)
  if (deviceIndex !== -1) {
    // 使用响应式更新
    deviceStore.devices[deviceIndex] = {
      ...deviceStore.devices[deviceIndex],
      latency: data.latency,
      last_latency_check: data.last_latency_check,
      status: data.status
    }
  }
}
```

---

### Task 6: 添加WebSocket连接状态指示器

**Files:**
- Modify: `frontend/src/views/DeviceManagement.vue`

**Step 1: 在页面添加连接状态指示**

```vue
<!-- 在搜索表单上方添加 -->
<div class="websocket-status">
  <el-tag 
    :type="websocketStore.isConnected ? 'success' : 'danger'" 
    size="small"
  >
    {{ websocketStore.isConnected ? '实时更新已连接' : '实时更新未连接' }}
  </el-tag>
</div>
```

**Step 2: 添加样式**

```css
.websocket-status {
  display: flex;
  justify-content: flex-end;
  margin-bottom: 10px;
}
```

---

## 依赖关系

```
Task 1 (WebSocket管理器)
    │
    ├──▶ Task 2 (WebSocket端点)
    │         │
    │         └──▶ Task 3 (延迟服务集成)
    │
    └──▶ Task 4 (前端Store)
              │
              └──▶ Task 5 (页面集成)
                        │
                        └──▶ Task 6 (状态指示器)
```

---

## 风险与缓解措施

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| WebSocket连接断开 | 用户无法收到实时更新 | 实现自动重连机制，最多重试5次 |
| 多标签页同时连接 | 服务器资源消耗 | 连接管理器支持多连接，消息广播 |
| 异步事件循环冲突 | 广播失败 | 检测事件循环状态，安全创建任务 |
| 浏览器兼容性 | 部分浏览器不支持 | WebSocket API 兼容性良好，无需特殊处理 |

---

## 验收标准

1. **功能验收**
   - [ ] 后端WebSocket端点可正常连接
   - [ ] 延迟检测完成后，前端实时收到更新
   - [ ] 设备列表延迟数据自动更新，无需手动刷新
   - [ ] 连接断开后自动重连

2. **性能验收**
   - [ ] WebSocket连接不影响页面加载速度
   - [ ] 消息推送延迟 < 1秒
   - [ ] 内存占用无明显增长

3. **稳定性验收**
   - [ ] 长时间运行无内存泄漏
   - [ ] 网络波动后自动恢复
   - [ ] 多标签页同时使用正常
