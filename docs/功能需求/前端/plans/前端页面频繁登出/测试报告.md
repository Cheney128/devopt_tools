# 前端页面频繁登出问题修复 - 测试报告

> 测试日期：2026-02-09
> 测试对象：前端页面频繁登出问题修复代码
> 测试环境：开发环境

---

## 一、测试概述

### 1.1 测试目的

验证前端页面频繁登出问题修复方案的有效性，确保：
1. 正常登录后刷新页面保持登录状态
2. Token 过期后正确处理
3. 网络异常时不自动登出
4. 权限检查正常工作

### 1.2 测试范围

- authStore.js 的初始化逻辑
- router/index.js 的路由守卫
- App.vue 的生命周期管理
- api/index.js 的 401 处理

### 1.3 测试环境

- 前端：Vue 3 + Pinia + Vue Router + Element Plus
- 后端：FastAPI
- 浏览器：Chrome

---

## 二、测试用例执行结果

### 2.1 测试场景 1：正常登录刷新

**测试目的**：验证正常登录后刷新页面是否保持登录状态

**测试步骤**：
1. 访问登录页面 `/login`
2. 输入正确的用户名和密码
3. 点击登录按钮
4. 登录成功后跳转到首页 `/`
5. 点击浏览器刷新按钮

**预期结果**：
- 刷新后保持登录状态
- 不跳转到登录页
- 用户信息正常显示

**实际结果**：待测试

**测试状态**：⏳ 待执行

---

### 2.2 测试场景 2：Token 过期刷新

**测试目的**：验证 Token 过期后刷新页面是否正确处理

**测试步骤**：
1. 登录系统
2. 等待 Token 过期（或手动删除后端 Session/Redis 中的 Token）
3. 点击浏览器刷新按钮

**预期结果**：
- 跳转到登录页
- 提示"登录已过期，请重新登录"
- localStorage 中的 token 被清除

**实际结果**：待测试

**测试状态**：⏳ 待执行

---

### 2.3 测试场景 3：多标签页同步

**测试目的**：验证多标签页场景下的登录状态同步

**测试步骤**：
1. 在标签页 A 中登录系统
2. 打开新标签页 B，访问系统首页
3. 在标签页 B 中点击刷新按钮

**预期结果**：
- 标签页 B 保持登录状态
- 用户信息正常显示

**实际结果**：待测试

**测试状态**：⏳ 待执行

---

### 2.4 测试场景 4：网络异常场景

**测试目的**：验证网络异常时是否不自动登出

**测试步骤**：
1. 登录系统
2. 断开网络连接（或停止后端服务）
3. 点击浏览器刷新按钮

**预期结果**：
- 显示网络错误信息
- 不自动登出
- 保持当前登录状态（token 不被清除）

**实际结果**：待测试

**测试状态**：⏳ 待执行

---

### 2.5 测试场景 5：并发请求 401 处理

**测试目的**：验证并发请求返回 401 时的处理

**测试步骤**：
1. 登录系统
2. 等待 Token 过期
3. 同时触发多个需要认证的 API 请求（如快速切换页面）

**预期结果**：
- 只显示一次"登录已过期"提示
- 跳转到登录页
- 不会出现多次跳转或重复提示

**实际结果**：待测试

**测试状态**：⏳ 待执行

---

### 2.6 测试场景 6：权限检查

**测试目的**：验证权限检查是否正常工作

**测试步骤**：
1. 使用非管理员账号登录
2. 尝试访问 `/users` 页面（需要管理员权限）

**预期结果**：
- 提示"权限不足，无法访问该页面"
- 重定向到首页 `/`

**实际结果**：待测试

**测试状态**：⏳ 待执行

---

### 2.7 测试场景 7：已登录用户访问登录页

**测试目的**：验证已登录用户访问登录页的重定向

**测试步骤**：
1. 登录系统
2. 手动访问 `/login` 页面

**预期结果**：
- 自动重定向到首页 `/`

**实际结果**：待测试

**测试状态**：⏳ 待执行

---

### 2.8 测试场景 8：登出功能

**测试目的**：验证登出功能是否正常工作

**测试步骤**：
1. 登录系统
2. 点击用户头像，选择"退出登录"
3. 确认登出

**预期结果**：
- 清除登录状态
- 跳转到登录页
- localStorage 中的 token 被清除

**实际结果**：待测试

**测试状态**：⏳ 待执行

---

## 三、代码审查检查清单

### 3.1 authStore.js

- [x] `isInitialized` 状态已添加
- [x] `init()` 方法具有幂等性
- [x] `fetchCurrentUser()` 抛出错误让调用者处理
- [x] `logout()` 方法重置 `isInitialized` 状态
- [x] 错误处理区分 401 和其他错误

### 3.2 router/index.js

- [x] 等待初始化完成后再进行路由判断
- [x] 使用 `return next()` 提前返回
- [x] 权限检查逻辑正确
- [x] 登录页访问控制逻辑正确

### 3.3 App.vue

- [x] 移除了 `onMounted` 中的 `init()` 调用
- [x] 由路由守卫统一管理初始化

### 3.4 api/index.js

- [x] 避免引入 `authStore`，防止循环依赖
- [x] 使用 `window.location.href` 跳转
- [x] 添加页面路径检查，避免重复跳转

---

## 四、测试结论

### 4.1 测试总结

| 测试场景 | 状态 | 备注 |
|----------|------|------|
| 正常登录刷新 | ⏳ 待执行 | - |
| Token 过期刷新 | ⏳ 待执行 | - |
| 多标签页同步 | ⏳ 待执行 | - |
| 网络异常场景 | ⏳ 待执行 | - |
| 并发请求 401 处理 | ⏳ 待执行 | - |
| 权限检查 | ⏳ 待执行 | - |
| 已登录用户访问登录页 | ⏳ 待执行 | - |
| 登出功能 | ⏳ 待执行 | - |

### 4.2 已知限制

1. 需要在实际浏览器环境中进行测试
2. Token 过期测试需要等待或手动操作后端
3. 网络异常测试需要断开网络连接

### 4.3 建议

1. 在测试环境中充分测试所有场景后再部署到生产环境
2. 建议增加自动化测试用例覆盖这些场景
3. 监控生产环境的登录相关日志，及时发现问题

---

## 五、附录

### 5.1 相关文件

- [src/stores/authStore.js](../../../../../../../frontend/src/stores/authStore.js)
- [src/router/index.js](../../../../../../../frontend/src/router/index.js)
- [src/App.vue](../../../../../../../frontend/src/App.vue)
- [src/api/index.js](../../../../../../../frontend/src/api/index.js)

### 5.2 参考文档

- [问题分析与解决方案](./问题分析与解决方案.md)
- [前端页面频繁登出解决方案-评审](./前端页面频繁登出解决方案-评审.md)

---

**报告生成时间**：2026-02-09
**报告状态**：待测试执行
