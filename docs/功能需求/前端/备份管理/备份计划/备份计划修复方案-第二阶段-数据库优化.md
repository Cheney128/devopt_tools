# 备份计划修复方案 - 第二阶段：数据库优化

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-02-23 |
| 实施周期 | 1-2周 |
| 方案类型 | 数据库优化方案（方案B） |
| 前置条件 | 第一阶段已完成并稳定运行 |

---

## 一、实施目标

本阶段目标是在第一阶段快速修复的基础上，进行数据库层面的优化，提升查询性能，完善数据模型。

### 1.1 优化目标清单

| 序号 | 优化目标 | 预期效果 |
|------|----------|----------|
| 1 | 添加`last_run_time`字段到`backup_schedules`表 | 查询性能提升，无需关联查询 |
| 2 | 优化执行日志查询性能 | 减少查询耗时 |
| 3 | 统一数据更新逻辑 | 确保数据一致性 |
| 4 | 添加必要的索引 | 提升查询效率 |

### 1.2 与第一阶段的关系

```
┌─────────────────────────────────────────────────────────────┐
│                     备份计划修复方案                         │
├─────────────────────────────────────────────────────────────┤
│  第一阶段（快速修复）                                        │
│  ├── 修复时间字段问题（已完成）                              │
│  ├── 修复last_run_time查询（已完成）                        │
│  └── 修复创建时日志记录（已完成）                            │
├─────────────────────────────────────────────────────────────┤
│  第二阶段（数据库优化）← 当前阶段                            │
│  ├── 添加last_run_time字段                                  │
│  ├── 优化查询性能                                           │
│  ├── 添加索引                                               │
│  └── 统一更新逻辑                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、数据库变更详情

### 2.1 添加`last_run_time`字段

#### 2.1.1 数据库迁移脚本

**创建迁移文件**：`migrations/versions/xxx_add_last_run_time_to_backup_schedules.py`

```python
"""add last_run_time to backup_schedules

Revision ID: xxx
Revises: yyy
Create Date: 2026-02-23

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import mysql

# revision identifiers, used by Alembic.
revision = 'xxx'
down_revision = 'yyy'
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column('backup_schedules', sa.Column('last_run_time', sa.DateTime(), nullable=True))
    op.create_index('ix_backup_schedules_last_run_time', 'backup_schedules', ['last_run_time'], unique=False)
    
    # 迁移现有数据：从backup_execution_logs表同步最后执行时间
    op.execute("""
        UPDATE backup_schedules bs
        SET last_run_time = (
            SELECT MAX(completed_at)
            FROM backup_execution_logs bel
            WHERE bel.schedule_id = bs.id
        )
        WHERE EXISTS (
            SELECT 1
            FROM backup_execution_logs bel
            WHERE bel.schedule_id = bs.id
        )
    """)
    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index('ix_backup_schedules_last_run_time', table_name='backup_schedules')
    op.drop_column('backup_schedules', 'last_run_time')
    # ### end Alembic commands ###
```

#### 2.1.2 修改数据库模型

**修改文件**：`app/models/models.py`

**位置**：第142-159行（BackupSchedule类）

**当前代码**：
```python
class BackupSchedule(Base):
    __tablename__ = "backup_schedules"
    
    id = Column(Integer, primary_key=True, index=True)
    device_id = Column(Integer, ForeignKey("devices.id"), nullable=False)
    schedule_type = Column(String(20), default="daily")  # hourly/daily/monthly
    time = Column(String(10), nullable=True)  # 时间点，格式 HH:MM
    day = Column(Integer, nullable=True)  # 每月备份日期，1-31
    is_active = Column(Boolean, default=True)
    created_at = Column(DateTime, default=func.now())
    updated_at = Column(DateTime, default=func.now(), onupdate=func.now())
```

**修改为**：
```python
class BackupSchedule(Base):
    __tablename__ = "backup_schedules"
    
    id = Column(Integer, primary_key=True, index=True)
    device_id = Column(Integer, ForeignKey("devices.id"), nullable=False)
    schedule_type = Column(String(20), default="daily")  # hourly/daily/monthly
    time = Column(String(10), nullable=True)  # 时间点，格式 HH:MM
    day = Column(Integer, nullable=True)  # 每月备份日期，1-31
    is_active = Column(Boolean, default=True)
    last_run_time = Column(DateTime, nullable=True, index=True)  # 上次执行时间
    created_at = Column(DateTime, default=func.now())
    updated_at = Column(DateTime, default=func.now(), onupdate=func.now())
```

---

### 2.2 添加索引优化

#### 2.2.1 执行日志表索引优化

**修改文件**：`app/models/models.py`

**位置**：第161-207行（BackupExecutionLog类）

**当前代码**：
```python
class BackupExecutionLog(Base):
    __tablename__ = "backup_execution_logs"
    
    id = Column(Integer, primary_key=True, index=True)
    task_id = Column(String(50), nullable=False, index=True)
    device_id = Column(Integer, ForeignKey("devices.id"), nullable=False)
    schedule_id = Column(Integer, ForeignKey("backup_schedules.id"), nullable=True)
    status = Column(String(20), nullable=False)
    execution_time = Column(Float, nullable=True)
    trigger_type = Column(String(20), default="scheduled")
    config_id = Column(Integer, ForeignKey("configurations.id"), nullable=True)
    error_message = Column(Text, nullable=True)
    started_at = Column(DateTime, nullable=True)
    completed_at = Column(DateTime, nullable=True)
    config_size = Column(Integer, nullable=True)
    git_commit_id = Column(String(40), nullable=True)
    created_at = Column(DateTime, default=func.now())
```

**修改为**：
```python
class BackupExecutionLog(Base):
    __tablename__ = "backup_execution_logs"
    
    id = Column(Integer, primary_key=True, index=True)
    task_id = Column(String(50), nullable=False, index=True)
    device_id = Column(Integer, ForeignKey("devices.id"), nullable=False, index=True)
    schedule_id = Column(Integer, ForeignKey("backup_schedules.id"), nullable=True, index=True)
    status = Column(String(20), nullable=False, index=True)
    execution_time = Column(Float, nullable=True)
    trigger_type = Column(String(20), default="scheduled")
    config_id = Column(Integer, ForeignKey("configurations.id"), nullable=True)
    error_message = Column(Text, nullable=True)
    started_at = Column(DateTime, nullable=True, index=True)
    completed_at = Column(DateTime, nullable=True, index=True)
    config_size = Column(Integer, nullable=True)
    git_commit_id = Column(String(40), nullable=True)
    created_at = Column(DateTime, default=func.now(), index=True)
    
    # 添加复合索引优化查询
    __table_args__ = (
        Index('ix_backup_execution_logs_schedule_completed', 'schedule_id', 'completed_at'),
        Index('ix_backup_execution_logs_device_created', 'device_id', 'created_at'),
    )
```

**创建索引迁移脚本**：

```python
"""add indexes to backup_execution_logs

Revision ID: xxx
Revises: yyy
Create Date: 2026-02-23

"""
from alembic import op
import sqlalchemy as sa

# revision identifiers, used by Alembic.
revision = 'xxx'
down_revision = 'yyy'
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_index('ix_backup_execution_logs_device_id', 'backup_execution_logs', ['device_id'], unique=False)
    op.create_index('ix_backup_execution_logs_schedule_id', 'backup_execution_logs', ['schedule_id'], unique=False)
    op.create_index('ix_backup_execution_logs_status', 'backup_execution_logs', ['status'], unique=False)
    op.create_index('ix_backup_execution_logs_started_at', 'backup_execution_logs', ['started_at'], unique=False)
    op.create_index('ix_backup_execution_logs_completed_at', 'backup_execution_logs', ['completed_at'], unique=False)
    op.create_index('ix_backup_execution_logs_created_at', 'backup_execution_logs', ['created_at'], unique=False)
    op.create_index('ix_backup_execution_logs_schedule_completed', 'backup_execution_logs', ['schedule_id', 'completed_at'], unique=False)
    op.create_index('ix_backup_execution_logs_device_created', 'backup_execution_logs', ['device_id', 'created_at'], unique=False)
    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index('ix_backup_execution_logs_device_created', table_name='backup_execution_logs')
    op.drop_index('ix_backup_execution_logs_schedule_completed', table_name='backup_execution_logs')
    op.drop_index('ix_backup_execution_logs_created_at', table_name='backup_execution_logs')
    op.drop_index('ix_backup_execution_logs_completed_at', table_name='backup_execution_logs')
    op.drop_index('ix_backup_execution_logs_started_at', table_name='backup_execution_logs')
    op.drop_index('ix_backup_execution_logs_status', table_name='backup_execution_logs')
    op.drop_index('ix_backup_execution_logs_schedule_id', table_name='backup_execution_logs')
    op.drop_index('ix_backup_execution_logs_device_id', table_name='backup_execution_logs')
    # ### end Alembic commands ###
```

---

## 三、API层修改详情

### 3.1 修改获取备份计划列表

**修改文件**：`app/api/endpoints/configurations.py`

**修改`get_backup_schedules`函数**

**位置**：第80-131行

**第一阶段代码**（使用关联查询）：
```python
@router.get("/backup-schedules", response_model=List[Dict])
async def get_backup_schedules(
    skip: int = 0,
    limit: int = 100,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    """获取备份计划列表"""
    try:
        results = db.query(BackupSchedule, Device.name).join(
            Device, BackupSchedule.device_id == Device.id
        ).offset(skip).limit(limit).all()
        
        # 批量查询每个计划的最后执行时间
        schedule_ids = [schedule.id for schedule, _ in results]
        last_executions = db.query(
            BackupExecutionLog.schedule_id,
            func.max(BackupExecutionLog.completed_at).label('last_run')
        ).filter(
            BackupExecutionLog.schedule_id.in_(schedule_ids)
        ).group_by(BackupExecutionLog.schedule_id).all()
        
        last_run_dict = {se.schedule_id: se.last_run for se in last_executions}
        
        schedules = []
        for schedule, device_name in results:
            schedule_dict = {
                'id': schedule.id,
                'device_id': schedule.device_id,
                'device_name': device_name,
                'schedule_type': schedule.schedule_type,
                'schedule_time': schedule.time,
                'schedule_day': schedule.day,
                'is_active': schedule.is_active,
                'last_run_time': last_run_dict.get(schedule.id),
                'created_at': schedule.created_at,
                'updated_at': schedule.updated_at
            }
            schedules.append(schedule_dict)
        
        return schedules
    except Exception as e:
        logger.error(f"获取备份计划列表失败: {str(e)}")
        raise HTTPException(status_code=500, detail=f"获取备份计划列表失败: {str(e)}")
```

**第二阶段代码**（直接从表字段读取）：
```python
@router.get("/backup-schedules", response_model=List[Dict])
async def get_backup_schedules(
    skip: int = 0,
    limit: int = 100,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    """获取备份计划列表"""
    try:
        results = db.query(BackupSchedule, Device.name).join(
            Device, BackupSchedule.device_id == Device.id
        ).offset(skip).limit(limit).all()
        
        schedules = []
        for schedule, device_name in results:
            schedule_dict = {
                'id': schedule.id,
                'device_id': schedule.device_id,
                'device_name': device_name,
                'schedule_type': schedule.schedule_type,
                'schedule_time': schedule.time,
                'schedule_day': schedule.day,
                'is_active': schedule.is_active,
                'last_run_time': schedule.last_run_time,  # 直接从表字段读取
                'created_at': schedule.created_at,
                'updated_at': schedule.updated_at
            }
            schedules.append(schedule_dict)
        
        return schedules
    except Exception as e:
        logger.error(f"获取备份计划列表失败: {str(e)}")
        raise HTTPException(status_code=500, detail=f"获取备份计划列表失败: {str(e)}")
```

### 3.2 修改获取单个备份计划

**修改`get_backup_schedule`函数**

**位置**：第134-166行

**第一阶段代码**（使用关联查询）：
```python
@router.get("/backup-schedules/{schedule_id}", response_model=Dict)
async def get_backup_schedule(
    schedule_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    """获取单个备份计划详情"""
    try:
        result = db.query(BackupSchedule, Device.name).join(
            Device, BackupSchedule.device_id == Device.id
        ).filter(BackupSchedule.id == schedule_id).first()
        
        if not result:
            raise HTTPException(status_code=404, detail="备份计划不存在")
        
        schedule, device_name = result
        
        # 查询该计划的最后执行时间
        last_execution = db.query(
            func.max(BackupExecutionLog.completed_at).label('last_run')
        ).filter(
            BackupExecutionLog.schedule_id == schedule_id
        ).scalar()
        
        return {
            'id': schedule.id,
            'device_id': schedule.device_id,
            'device_name': device_name,
            'schedule_type': schedule.schedule_type,
            'schedule_time': schedule.time,
            'schedule_day': schedule.day,
            'is_active': schedule.is_active,
            'last_run_time': last_execution,
            'created_at': schedule.created_at,
            'updated_at': schedule.updated_at
        }
    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"获取备份计划详情失败: {str(e)}")
        raise HTTPException(status_code=500, detail=f"获取备份计划详情失败: {str(e)}")
```

**第二阶段代码**（直接从表字段读取）：
```python
@router.get("/backup-schedules/{schedule_id}", response_model=Dict)
async def get_backup_schedule(
    schedule_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    """获取单个备份计划详情"""
    try:
        result = db.query(BackupSchedule, Device.name).join(
            Device, BackupSchedule.device_id == Device.id
        ).filter(BackupSchedule.id == schedule_id).first()
        
        if not result:
            raise HTTPException(status_code=404, detail="备份计划不存在")
        
        schedule, device_name = result
        
        return {
            'id': schedule.id,
            'device_id': schedule.device_id,
            'device_name': device_name,
            'schedule_type': schedule.schedule_type,
            'schedule_time': schedule.time,
            'schedule_day': schedule.day,
            'is_active': schedule.is_active,
            'last_run_time': schedule.last_run_time,  # 直接从表字段读取
            'created_at': schedule.created_at,
            'updated_at': schedule.updated_at
        }
    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"获取备份计划详情失败: {str(e)}")
        raise HTTPException(status_code=500, detail=f"获取备份计划详情失败: {str(e)}")
```

---

### 3.3 修改立即备份函数

**修改文件**：`app/api/endpoints/configurations.py`

**修改`backup_now`函数**

**位置**：第598-682行

**在记录执行日志后，添加更新`last_run_time`的逻辑**：

```python
@router.post("/backup-schedules/{schedule_id}/backup-now", response_model=Dict)
async def backup_now(
    schedule_id: int,
    background_tasks: BackgroundTasks,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
    netmiko_service: NetmikoService = Depends(get_netmiko_service),
    git_service: GitService = Depends(get_git_service)
):
    """立即执行备份"""
    try:
        # 获取备份计划
        schedule = db.query(BackupSchedule).filter(BackupSchedule.id == schedule_id).first()
        if not schedule:
            raise HTTPException(status_code=404, detail="备份计划不存在")
        
        device = db.query(Device).filter(Device.id == schedule.device_id).first()
        if not device:
            raise HTTPException(status_code=404, detail="设备不存在")
        
        # 执行任务
        task_id = str(uuid.uuid4())
        started_at = datetime.now()
        
        try:
            result = await collect_config_from_device(
                device.id, db, netmiko_service, git_service
            )
            
            # 创建执行日志
            execution_log = BackupExecutionLog(
                task_id=task_id,
                device_id=device.id,
                schedule_id=schedule.id,
                status="success" if result.get("success") else "failed",
                trigger_type="manual",
                error_message=result.get("error"),
                started_at=started_at,
                completed_at=datetime.now(),
                config_id=result.get("config_id")
            )
            db.add(execution_log)
            
            # 更新备份计划的last_run_time
            if result.get("success"):
                schedule.last_run_time = datetime.now()
            
            db.commit()
            
            return {
                "success": result.get("success"),
                "message": "备份执行成功" if result.get("success") else f"备份执行失败: {result.get('error')}",
                "task_id": task_id,
                "result": result
            }
            
        except Exception as backup_error:
            # 记录失败日志
            execution_log = BackupExecutionLog(
                task_id=task_id,
                device_id=device.id,
                schedule_id=schedule.id,
                status="failed",
                trigger_type="manual",
                error_message=str(backup_error),
                started_at=started_at,
                completed_at=datetime.now()
            )
            db.add(execution_log)
            db.commit()
            
            logger.error(f"立即备份失败: {str(backup_error)}")
            return {
                "success": False,
                "message": f"备份执行失败: {str(backup_error)}",
                "task_id": task_id
            }
            
    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"立即备份失败: {str(e)}")
        raise HTTPException(status_code=500, detail=f"立即备份失败: {str(e)}")
```

---

### 3.4 修改调度器服务

**修改文件**：`app/services/backup_scheduler.py`

**在调度执行备份后，添加更新`last_run_time`的逻辑**

**位置**：第144-226行（execute_backup函数）

```python
async def execute_backup(schedule_id: int, db: Session):
    """执行备份任务"""
    try:
        # 获取备份计划
        schedule = db.query(BackupSchedule).filter(BackupSchedule.id == schedule_id).first()
        if not schedule or not schedule.is_active:
            logger.warning(f"备份计划不存在或未激活: {schedule_id}")
            return
        
        device = db.query(Device).filter(Device.id == schedule.device_id).first()
        if not device:
            logger.warning(f"设备不存在: {schedule.device_id}")
            return
        
        # 执行任务
        task_id = str(uuid.uuid4())
        started_at = datetime.now()
        
        try:
            # 这里调用实际的备份逻辑
            result = await collect_config_from_device(
                device.id, db, netmiko_service, git_service
            )
            
            # 创建执行日志
            execution_log = BackupExecutionLog(
                task_id=task_id,
                device_id=device.id,
                schedule_id=schedule.id,
                status="success" if result.get("success") else "failed",
                trigger_type="scheduled",
                error_message=result.get("error"),
                started_at=started_at,
                completed_at=datetime.now(),
                config_id=result.get("config_id")
            )
            db.add(execution_log)
            
            # 更新备份计划的last_run_time
            if result.get("success"):
                schedule.last_run_time = datetime.now()
            
            db.commit()
            
            logger.info(f"定时备份执行成功: schedule_id={schedule_id}, task_id={task_id}")
            
        except Exception as backup_error:
            # 记录失败日志
            execution_log = BackupExecutionLog(
                task_id=task_id,
                device_id=device.id,
                schedule_id=schedule.id,
                status="failed",
                trigger_type="scheduled",
                error_message=str(backup_error),
                started_at=started_at,
                completed_at=datetime.now()
            )
            db.add(execution_log)
            db.commit()
            
            logger.error(f"定时备份执行失败: schedule_id={schedule_id}, error={str(backup_error)}")
            
    except Exception as e:
        logger.error(f"执行备份任务失败: schedule_id={schedule_id}, error={str(e)}")
```

---

### 3.5 修改创建备份计划函数

**修改文件**：`app/api/endpoints/configurations.py`

**修改`create_backup_schedule`函数**

**在记录执行日志后，添加更新`last_run_time`的逻辑**：

```python
@router.post("/backup-schedules", response_model=Dict)
async def create_backup_schedule(
    schedule_data: BackupScheduleCreate,
    background_tasks: BackgroundTasks,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
    netmiko_service: NetmikoService = Depends(get_netmiko_service),
    git_service: GitService = Depends(get_git_service)
):
    """创建备份计划"""
    try:
        # 创建设备备份目录
        device = db.query(Device).filter(Device.id == schedule_data.device_id).first()
        if not device:
            raise HTTPException(status_code=404, detail="设备不存在")
        
        device_backup_dir = os.path.join(BACKUP_DIR, device.ip_address.replace('.', '_'))
        os.makedirs(device_backup_dir, exist_ok=True)
        
        # 创建备份计划
        schedule = BackupSchedule(
            device_id=schedule_data.device_id,
            schedule_type=schedule_data.schedule_type,
            time=schedule_data.time,
            day=schedule_data.day,
            is_active=schedule_data.is_active
        )
        db.add(schedule)
        db.commit()
        db.refresh(schedule)
        
        # 立即执行一次备份
        task_id = str(uuid.uuid4())
        started_at = datetime.now()
        
        try:
            backup_result = await collect_config_from_device(
                schedule.device_id, db, netmiko_service, git_service
            )
            
            # 记录执行日志
            execution_log = BackupExecutionLog(
                task_id=task_id,
                device_id=schedule.device_id,
                schedule_id=schedule.id,
                status="success" if backup_result.get("success") else "failed",
                trigger_type="manual",
                error_message=backup_result.get("error"),
                started_at=started_at,
                completed_at=datetime.now(),
                config_id=backup_result.get("config_id")
            )
            db.add(execution_log)
            
            # 更新备份计划的last_run_time
            if backup_result.get("success"):
                schedule.last_run_time = datetime.now()
                db.commit()
            else:
                db.commit()
            
        except Exception as backup_error:
            # 备份执行失败，记录失败日志
            logger.error(f"创建备份计划时立即备份失败: {str(backup_error)}")
            execution_log = BackupExecutionLog(
                task_id=task_id,
                device_id=schedule.device_id,
                schedule_id=schedule.id,
                status="failed",
                trigger_type="manual",
                error_message=str(backup_error),
                started_at=started_at,
                completed_at=datetime.now()
            )
            db.add(execution_log)
            db.commit()
            backup_result = {"success": False, "error": str(backup_error)}
        
        return {
            "id": schedule.id,
            "message": "备份计划创建成功",
            "initial_backup": backup_result
        }
    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"创建备份计划失败: {str(e)}")
        raise HTTPException(status_code=500, detail=f"创建备份计划失败: {str(e)}")
```

---

## 四、测试验证方案

### 4.1 数据库迁移测试

#### 测试1：迁移脚本执行测试

**测试目标**：验证数据库迁移脚本能正确执行

**测试步骤**：
1. 在测试数据库执行迁移脚本
2. 验证`backup_schedules`表新增`last_run_time`字段
3. 验证索引正确创建
4. 验证现有数据正确迁移

**预期结果**：
- 字段添加成功
- 索引创建成功
- 现有计划的`last_run_time`被正确填充

#### 测试2：回滚测试

**测试目标**：验证迁移可以正确回滚

**测试步骤**：
1. 执行迁移脚本
2. 执行回滚命令
3. 验证`last_run_time`字段被删除
4. 验证索引被删除

**预期结果**：回滚成功，数据库恢复到迁移前状态

### 4.2 功能测试

#### 测试3：创建备份计划时last_run_time更新测试

**测试目标**：验证创建备份计划时`last_run_time`正确更新

**测试步骤**：
1. 创建一个新的备份计划
2. 查询数据库验证`last_run_time`字段被设置为当前时间
3. 调用API获取备份计划，验证`last_run_time`正确返回

**预期结果**：`last_run_time`正确更新和返回

#### 测试4：立即备份时last_run_time更新测试

**测试目标**：验证立即备份时`last_run_time`正确更新

**测试步骤**：
1. 选择一个现有备份计划，记录当前`last_run_time`
2. 点击"立即备份"按钮
3. 备份成功后，验证`last_run_time`更新为当前时间

**预期结果**：`last_run_time`正确更新

#### 测试5：定时备份时last_run_time更新测试

**测试目标**：验证定时备份时`last_run_time`正确更新

**测试步骤**：
1. 创建一个每分钟执行的备份计划
2. 等待定时任务执行
3. 验证`last_run_time`更新为执行时间

**预期结果**：`last_run_time`正确更新

### 4.3 性能测试

#### 测试6：查询性能对比测试

**测试目标**：对比第一阶段和第二阶段的查询性能

**测试步骤**：
1. 准备1000条备份计划数据
2. 使用第一阶段代码查询，记录耗时
3. 使用第二阶段代码查询，记录耗时
4. 对比两者性能差异

**预期结果**：第二阶段查询性能优于第一阶段

**性能对比表**：

| 数据量 | 第一阶段耗时 | 第二阶段耗时 | 性能提升 |
|--------|-------------|-------------|---------|
| 100条 | - | - | - |
| 500条 | - | - | - |
| 1000条 | - | - | - |

### 4.4 回归测试

#### 测试7：所有备份计划相关API测试

**测试目标**：验证所有备份计划相关API正常工作

**测试范围**：
- 创建备份计划
- 获取备份计划列表
- 获取单个备份计划
- 更新备份计划
- 删除备份计划
- 立即备份
- 获取执行日志

**预期结果**：所有API正常工作

---

## 五、实施步骤

### 5.1 准备工作

1. **确认第一阶段已稳定运行**
   - 检查第一阶段修复是否正常工作
   - 确认无遗留问题

2. **备份数据库**：
   ```bash
   mysqldump -u username -p database_name > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

3. **备份代码**：
   ```bash
   git add .
   git commit -m "backup: 第二阶段实施前代码备份"
   ```

4. **创建功能分支**：
   ```bash
   git checkout -b feature/backup-schedule-phase2
   ```

### 5.2 代码修改

按照"二、数据库变更详情"和"三、API层修改详情"中的说明，依次修改以下文件：

1. `app/models/models.py` - 添加字段和索引
2. `app/api/endpoints/configurations.py` - 修改API实现
3. `app/services/backup_scheduler.py` - 修改调度器服务

### 5.3 创建迁移脚本

1. 生成迁移脚本：
   ```bash
   alembic revision --autogenerate -m "add last_run_time to backup_schedules"
   ```

2. 检查生成的迁移脚本，确保正确

3. 手动创建索引迁移脚本（如需要）

### 5.4 本地测试

1. **执行数据库迁移**：
   ```bash
   alembic upgrade head
   ```

2. **启动后端服务**：
   ```bash
   cd backend
   uvicorn app.main:app --reload
   ```

3. **执行单元测试**：
   ```bash
   pytest tests/test_backup_schedules.py -v
   ```

4. **执行性能测试**：对比第一阶段和第二阶段性能

5. **手动验证**：按照"四、测试验证方案"进行手动测试

### 5.5 代码提交

1. **记录变更**：
   ```bash
   git diff > docs/变更记录/backup-schedule-phase2.diff
   ```

2. **更新变更文档**：按照项目规范更新`docs/diff-change.md`

3. **提交代码**：
   ```bash
   git add .
   git commit -m "feat(backup): 添加last_run_time字段优化备份计划查询性能

   - 在backup_schedules表添加last_run_time字段
   - 为backup_execution_logs表添加索引优化查询
   - 修改API层直接从表字段读取last_run_time
   - 统一更新逻辑确保数据一致性
   
   优化 #2"
   ```

### 5.6 部署上线

1. **部署到测试环境**
   - 执行数据库迁移
   - 部署新代码
   - 验证功能正常

2. **部署到生产环境**
   - 选择低峰期部署
   - 执行数据库迁移
   - 部署新代码
   - 监控服务状态

---

## 六、回滚方案

如果优化出现问题，可以按以下步骤回滚：

### 6.1 代码回滚

```bash
git revert <commit-hash>
```

### 6.2 数据库回滚

```bash
alembic downgrade -1
```

### 6.3 服务重启

```bash
# 重启后端服务
systemctl restart backup-scheduler
```

### 6.4 验证回滚

确认功能恢复到第二阶段实施前状态

---

## 七、风险评估

| 风险项 | 可能性 | 影响 | 应对措施 |
|--------|--------|------|----------|
| 数据库迁移失败 | 低 | 高 | 提前备份数据库，测试环境先验证 |
| 数据迁移不完整 | 低 | 高 | 迁移后验证数据完整性 |
| 索引创建导致锁表 | 中 | 中 | 选择低峰期执行，使用在线DDL工具 |
| 性能优化不达预期 | 低 | 中 | 保留第一阶段代码作为备选方案 |
| 数据一致性问题 | 低 | 高 | 添加数据校验机制，定期检查 |

---

## 八、附件

### 8.1 修改文件清单

| 文件路径 | 修改类型 | 说明 |
|---------|---------|------|
| `app/models/models.py` | 修改 | 添加last_run_time字段和索引 |
| `app/api/endpoints/configurations.py` | 修改 | 修改3个函数实现 |
| `app/services/backup_scheduler.py` | 修改 | 修改execute_backup函数 |
| `migrations/versions/xxx_add_last_run_time.py` | 新增 | 数据库迁移脚本 |
| `migrations/versions/xxx_add_indexes.py` | 新增 | 索引迁移脚本 |

### 8.2 数据库变更汇总

**backup_schedules表**：
- 新增字段：`last_run_time` (DateTime, nullable)
- 新增索引：`ix_backup_schedules_last_run_time`

**backup_execution_logs表**：
- 新增索引：`ix_backup_execution_logs_device_id`
- 新增索引：`ix_backup_execution_logs_schedule_id`
- 新增索引：`ix_backup_execution_logs_status`
- 新增索引：`ix_backup_execution_logs_started_at`
- 新增索引：`ix_backup_execution_logs_completed_at`
- 新增索引：`ix_backup_execution_logs_created_at`
- 新增复合索引：`ix_backup_execution_logs_schedule_completed`
- 新增复合索引：`ix_backup_execution_logs_device_created`

### 8.3 性能基准

**优化前（第一阶段）**：
- 获取100条备份计划：约X ms
- 获取500条备份计划：约X ms
- 获取1000条备份计划：约X ms

**优化后（第二阶段）**：
- 获取100条备份计划：约X ms
- 获取500条备份计划：约X ms
- 获取1000条备份计划：约X ms

---

**文档编制日期**：2026-02-23  
**编制人**：AI Assistant  
**审核状态**：待审核  
**前置依赖**：《备份计划修复方案-第一阶段-快速修复.md》
