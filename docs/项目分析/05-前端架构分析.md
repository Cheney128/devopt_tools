# 交换机批量管理与巡检系统 - 前端架构分析

> 分析日期：2026-02-14（更新）
> 分析工具：SKILL (brainstorming, writing-plans, development-documentation-archiving)

---

## 0. 前端布局设计模拟

### 0.1 整体布局结构

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              顶部导航栏 (Header) - 60px                              │
│  ┌─────────────────────────────────────────────────────────────────────────────────┐│
│  │  📡 交换机管理系统                                    👤 用户昵称 ▼           ││
│  │                                                        ├─ 个人中心              ││
│  │                                                        └─ 退出登录              ││
│  └─────────────────────────────────────────────────────────────────────────────────┘│
├───────────────────┬─────────────────────────────────────────────────────────────────┤
│                   │                                                                 │
│   侧边栏菜单       │                      主内容区域 (Main)                          │
│   (Sidebar)       │                                                                 │
│   200px           │    ┌─────────────────────────────────────────────────────────┐  │
│                   │    │                                                         │  │
│  ┌──────────────┐ │    │   根据 router-view 动态渲染的页面内容                    │  │
│  │ 🏠 首页      │ │    │                                                         │  │
│  ├──────────────┤ │    │   - HomeView          首页概览                          │  │
│  │ 🖥️ 设备管理  │ │    │   - DeviceManagement  设备管理                          │  │
│  ├──────────────┤ │    │   - PortManagement    端口管理                          │  │
│  │ 🔌 端口管理  │ │    │   - VLANManagement    VLAN管理                          │  │
│  ├──────────────┤ │    │   - Inspection...     巡检管理                          │  │
│  │ 📊 VLAN管理  │ │    │   - Configuration...  配置管理                          │  │
│  ├──────────────┤ │    │   - BackupSchedule... 备份计划                          │  │
│  │ 📋 巡检管理  │ │    │   - BackupMonitoring  备份监控                          │  │
│  ├──────────────┤ │    │   - DeviceCollection  设备采集                          │  │
│  │ 📁 配置管理  │ │    │   - GitConfig...      Git配置                          │  │
│  ├──────────────┤ │    │   - UserManagement    用户管理 (管理员)                 │  │
│  │ 📅 备份计划  │ │    │   - ProfileView       个人中心                          │  │
│  ├──────────────┤ │    │                                                         │  │
│  │ 📈 备份监控  │ │    └─────────────────────────────────────────────────────────┘  │
│  ├──────────────┤ │                                                                 │
│  │ 📡 设备采集  │ │                                                                 │
│  ├──────────────┤ │                                                                 │
│  │ 🔧 Git配置   │ │                                                                 │
│  ├──────────────┤ │                                                                 │
│  │ 👥 用户管理  │ │                                                                 │
│  │  (管理员)    │ │                                                                 │
│  └──────────────┘ │                                                                 │
│                   │                                                                 │
└───────────────────┴─────────────────────────────────────────────────────────────────┘
```

### 0.2 登录页面布局

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                                                                                     │
│                                                                                     │
│                                                                                     │
│                          ┌─────────────────────────────┐                           │
│                          │     交换机管理系统          │                           │
│                          │   Switch Management System  │                           │
│                          ├─────────────────────────────┤                           │
│                          │                             │                           │
│                          │  👤 [用户名____________]    │                           │
│                          │                             │                           │
│                          │  🔒 [密码______________]    │                           │
│                          │                             │                           │
│                          │  🔑 [验证码] [验证码图片]   │                           │
│                          │                             │                           │
│                          │  ☑ 记住我                   │                           │
│                          │                             │                           │
│                          │  [      登  录      ]       │                           │
│                          │                             │                           │
│                          │  默认账号: admin / admin123 │                           │
│                          └─────────────────────────────┘                           │
│                                                                                     │
│                                                                                     │
│                              渐变背景 (紫色系)                                       │
│                                                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 0.3 设备管理页面布局

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  设备管理                                                        操作按钮组         │
│                                                          [添加设备] [批量操作▼]    │
│                                                         [批量上传] [下载模板]      │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  搜索筛选:  [状态 ▼]  [厂商 ▼]  [搜索] [重置]                                       │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────┬────────────┬────────────┬────────┬──────────┬────────┬────────┬─────────┐ │
│  │ ☐  │ ID         │ 主机名      │ IP地址 │ 厂商     │ 型号   │ 状态   │ 操作    │ │
│  ├─────┼────────────┼────────────┼────────┼──────────┼────────┼────────┼─────────┤ │
│  │ ☐  │ 1          │ SW-Core-01 │ 192... │ 华为     │ S5720  │ 🟢活跃 │ 编辑 删除│ │
│  │     │            │            │        │          │        │        │ 测试 执行│ │
│  ├─────┼────────────┼────────────┼────────┼──────────┼────────┼────────┼─────────┤ │
│  │ ☐  │ 2          │ SW-Acc-01  │ 192... │ 思科     │ 2960   │ 🟡维护 │ 编辑 删除│ │
│  │     │            │            │        │          │        │        │ 测试 执行│ │
│  └─────┴────────────┴────────────┴────────┴──────────┴────────┴────────┴─────────┘ │
│                                                                                     │
│  共 100 条  [10条/页 ▼]  < 1 2 3 4 5 ... 10 >  前往 [  ] 页                         │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 0.4 备份监控页面布局

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  备份计划监控                                                          [刷新]       │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐            │
│  │   设备总数   │  │   备份计划   │  │    成功率    │  │   今日备份   │            │
│  │     100      │  │     25个     │  │    98.5%     │  │     15台     │            │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘            │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────┐  ┌────────────────────┐                │
│  │        备份趋势 (最近7天)              │  │   执行状态分布     │                │
│  │  ▇▇▇▇                                  │  │                    │                │
│  │  ▇▇▇▇ ▇▇▇▇                             │  │    ┌──────┐        │                │
│  │  ▇▇▇▇ ▇▇▇▇ ▇▇▇▇                        │  │   ╱ 成功  ╲       │                │
│  │  ▇▇▇▇ ▇▇▇▇ ▇▇▇▇ ▇▇▇▇                   │  │  │  98%   │       │                │
│  │  Mon  Tue  Wed  Thu  Fri  Sat  Sun     │  │   ╲ 失败 ╱        │                │
│  │  ■ 成功  ■ 失败                         │  │    └──────┘        │                │
│  └────────────────────────────────────────┘  └────────────────────┘                │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  最近执行日志                                                    [查看全部]        │
│  ┌────────────┬────────┬──────────┬────────┬──────────────┬──────────────────────┐ │
│  │ 设备       │ 状态   │ 触发类型 │ 耗时   │ 执行时间     │ 备注                 │ │
│  ├────────────┼────────┼──────────┼────────┼──────────────┼──────────────────────┤ │
│  │ SW-Core-01 │ 🟢成功 │ 计划     │ 2.35s  │ 2026-02-14   │ -                    │ │
│  │ SW-Acc-01  │ 🟢成功 │ 手动     │ 1.89s  │ 2026-02-14   │ 配置无变化           │ │
│  └────────────┴────────┴──────────┴────────┴──────────────┴──────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 0.5 个人中心页面布局

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  个人中心                                                                           │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────────────────────────┐│
│  │  [基本信息]  [修改密码]                                                          ││
│  ├─────────────────────────────────────────────────────────────────────────────────┤│
│  │                                                                                 ││
│  │      ┌──────┐                                                                   ││
│  │      │  👤  │  管理员昵称                                                       ││
│  │      │ 头像 │  @admin                                                           ││
│  │      └──────┘  🔴 管理员                                                        ││
│  │                                                                                 ││
│  │  ────────────────────────────────────────────────────────────────────────────  ││
│  │                                                                                 ││
│  │  用户名:    [admin         ] (不可修改)                                         ││
│  │  昵称:      [管理员________]                                                    ││
│  │  邮箱:      [admin@example.com]                                                 ││
│  │  手机号:    [13800138000____]                                                   ││
│  │                                                                                 ││
│  │  [保存修改]                                                                     ││
│  │                                                                                 ││
│  └─────────────────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 1. 前端架构概览

### 1.1 技术栈
- **框架**: Vue.js 3.5.24 (Composition API)
- **UI组件库**: Element Plus 2.13.1
- **状态管理**: Pinia 3.0.4
- **路由管理**: Vue Router 4.6.4
- **HTTP客户端**: Axios 1.13.2
- **图表库**: ECharts (备份监控)
- **构建工具**: Vite 7.2.4
- **测试框架**: Vitest 4.0.18

### 1.2 架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              前端架构图                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         视图层 (Views)                               │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │   │
│  │  │  首页    │ │ 设备管理 │ │ 端口管理 │ │ VLAN管理 │ │ 巡检管理 │  │   │
│  │  │HomeView  │ │ Device   │ │  Port    │ │  VLAN    │ │Inspection│  │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘  │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │   │
│  │  │ 配置管理 │ │ 备份计划 │ │ 备份监控 │ │ 设备采集 │ │ Git配置  │  │   │
│  │  │  Config  │ │ Schedule │ │Monitoring│ │Collection│ │  Git     │  │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘  │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐                            │   │
│  │  │ 用户管理 │ │ 个人中心 │ │ 登录页面 │                            │   │
│  │  │  User    │ │ Profile  │ │  Login   │                            │   │
│  │  └──────────┘ └──────────┘ └──────────┘                            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         状态管理层 (Store)                           │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐     │   │
│  │  │   deviceStore   │  │deviceCollection │  │   authStore     │     │   │
│  │  │   (设备状态)     │  │   (采集状态)     │  │   (认证状态)     │     │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         API服务层 (API)                              │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐     │   │
│  │  │   deviceApi     │  │ configurationApi │  │   authApi       │     │   │
│  │  │   (设备API)      │  │   (配置API)      │  │   (认证API)      │     │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘     │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐     │   │
│  │  │  monitoringApi  │  │deviceCollection │  │   gitConfigApi  │     │   │
│  │  │   (监控API)      │  │     Api         │  │   (Git API)      │     │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         HTTP客户端 (Axios)                           │   │
│  │            请求拦截器(Token注入) │ 响应拦截器 │ 错误处理              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         后端API (FastAPI)                            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 项目结构

### 2.1 目录结构

```
frontend/
├── public/                     # 静态资源
│   ├── vite.svg
│   ├── test.html
│   └── simple-test.html
│
├── src/
│   ├── api/                    # API服务层
│   │   ├── index.js           # Axios配置、拦截器和所有API导出
│   │   ├── auth.js            # 认证相关API (登录、验证码、用户管理)
│   │   └── deviceCollection.js # 设备采集API
│   │
│   ├── assets/                 # 资源文件
│   │   └── vue.svg
│   │
│   ├── components/             # 公共组件
│   │   └── HelloWorld.vue
│   │
│   ├── router/                 # 路由配置
│   │   └── index.js           # 路由定义和导航守卫
│   │
│   ├── stores/                 # 状态管理 (Pinia)
│   │   ├── authStore.js       # 认证状态管理 (登录、用户信息、验证码)
│   │   ├── deviceStore.js     # 设备状态管理
│   │   └── deviceCollectionStore.js # 设备采集状态管理
│   │
│   ├── views/                  # 页面视图
│   │   ├── LoginView.vue             # 登录页面 (验证码登录)
│   │   ├── HomeView.vue              # 首页
│   │   ├── DeviceManagement.vue      # 设备管理 (CRUD、批量操作、命令执行)
│   │   ├── PortManagement.vue        # 端口管理
│   │   ├── VLANManagement.vue        # VLAN管理
│   │   ├── InspectionManagement.vue  # 巡检管理
│   │   ├── ConfigurationManagement.vue # 配置管理
│   │   ├── BackupScheduleManagement.vue # 备份计划管理
│   │   ├── BackupMonitoring.vue      # 备份监控 (ECharts图表)
│   │   ├── DeviceCollection.vue      # 设备采集
│   │   ├── GitConfigManagement.vue   # Git配置管理
│   │   ├── UserManagement.vue        # 用户管理 (管理员)
│   │   └── ProfileView.vue           # 个人中心
│   │
│   ├── App.vue                 # 根组件 (布局框架)
│   ├── App-simple.vue          # 简化版根组件
│   ├── main.js                 # 入口文件
│   └── style.css               # 全局样式
│
├── test/                       # 测试文件
│   ├── setup.js               # 测试配置
│   ├── DeviceManagement.test.js # 设备管理测试
│   └── apiBaseUrl.test.js     # API基础URL测试
│
├── .gitignore                  # Git忽略配置
├── Dockerfile.frontend         # 前端Dockerfile
├── index.html                  # HTML入口
├── nginx.conf                  # Nginx配置
├── package.json                # 依赖配置
├── README.md                   # 项目文档
├── vite.config.js              # Vite配置
└── vitest.config.js            # Vitest配置
```

---

## 3. 核心模块详解

### 3.1 路由系统 (Vue Router)

**路由配置** (`src/router/index.js`):

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import { useAuthStore } from '../stores/authStore'
import { ElMessage } from 'element-plus'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/login',
      name: 'login',
      component: () => import('../views/LoginView.vue'),
      meta: { public: true }
    },
    {
      path: '/',
      name: 'home',
      component: () => import('../views/HomeView.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/devices',
      name: 'devices',
      component: () => import('../views/DeviceManagement.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/ports',
      name: 'ports',
      component: () => import('../views/PortManagement.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/vlans',
      name: 'vlans',
      component: () => import('../views/VLANManagement.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/inspections',
      name: 'inspections',
      component: () => import('../views/InspectionManagement.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/configurations',
      name: 'configurations',
      component: () => import('../views/ConfigurationManagement.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/monitoring',
      name: 'monitoring',
      component: () => import('../views/BackupMonitoring.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/backup-schedules',
      name: 'backup-schedules',
      component: () => import('../views/BackupScheduleManagement.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/device-collection',
      name: 'device-collection',
      component: () => import('../views/DeviceCollection.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/git-configs',
      name: 'git-configs',
      component: () => import('../views/GitConfigManagement.vue'),
      meta: { requiresAuth: true }
    },
    {
      path: '/users',
      name: 'users',
      component: () => import('../views/UserManagement.vue'),
      meta: { requiresAuth: true, requiresAdmin: true }
    },
    {
      path: '/profile',
      name: 'profile',
      component: () => import('../views/ProfileView.vue'),
      meta: { requiresAuth: true }
    }
  ]
})
```

**路由说明**:

| 路径 | 名称 | 组件 | 权限 | 说明 |
|------|------|------|------|------|
| /login | login | LoginView | 公开 | 登录页面 |
| / | home | HomeView | 需登录 | 系统首页 |
| /devices | devices | DeviceManagement | 需登录 | 设备管理页面 |
| /ports | ports | PortManagement | 需登录 | 端口管理页面 |
| /vlans | vlans | VLANManagement | 需登录 | VLAN管理页面 |
| /inspections | inspections | InspectionManagement | 需登录 | 巡检管理页面 |
| /configurations | configurations | ConfigurationManagement | 需登录 | 配置管理页面 |
| /backup-schedules | backup-schedules | BackupScheduleManagement | 需登录 | 备份计划页面 |
| /monitoring | monitoring | BackupMonitoring | 需登录 | 备份监控页面 |
| /device-collection | device-collection | DeviceCollection | 需登录 | 设备采集页面 |
| /git-configs | git-configs | GitConfigManagement | 需登录 | Git配置管理页面 |
| /users | users | UserManagement | 管理员 | 用户管理页面 |
| /profile | profile | ProfileView | 需登录 | 个人中心页面 |

**路由守卫**:

```javascript
router.beforeEach(async (to, from, next) => {
  const authStore = useAuthStore()

  // 等待初始化完成（只执行一次）
  if (!authStore.isInitialized && authStore.token) {
    await authStore.init()
  }

  const isLoggedIn = authStore.isLoggedIn

  // 1. 已登录用户访问登录页，重定向到首页
  if (to.path === '/login' && isLoggedIn) {
    return next('/')
  }

  // 2. 公开页面，直接访问
  if (to.meta.public) {
    return next()
  }

  // 3. 需要登录的页面
  if (to.meta.requiresAuth) {
    if (!isLoggedIn) {
      return next({
        path: '/login',
        query: { redirect: to.fullPath }
      })
    }

    // 4. 检查管理员权限
    if (to.meta.requiresAdmin && !authStore.isAdmin) {
      ElMessage.error('权限不足，无法访问该页面')
      return next('/')
    }
  }

  next()
})
```

### 3.2 状态管理 (Pinia)

#### 3.2.1 认证状态管理 (`src/stores/authStore.js`)

```javascript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { ElMessage } from 'element-plus'
import { authApi } from '../api/auth'

export const useAuthStore = defineStore('auth', () => {
  // State
  const token = ref(localStorage.getItem('token') || '')
  const user = ref(null)
  const isLoading = ref(false)
  const isInitialized = ref(false)
  const captchaId = ref('')
  const captchaImage = ref('')

  // Getters
  const isLoggedIn = computed(() => !!token.value)
  const isAdmin = computed(() => {
    if (!user.value || !user.value.roles) return false
    return user.value.roles.some(role => role.name === 'admin') || user.value.is_superuser
  })
  const username = computed(() => user.value?.username || '')
  const nickname = computed(() => user.value?.nickname || user.value?.username || '')
  const avatar = computed(() => user.value?.avatar || '')

  // Actions
  const fetchCaptcha = async () => { /* 获取验证码 */ }
  const login = async (credentials) => { /* 登录 */ }
  const fetchCurrentUser = async () => { /* 获取当前用户信息 */ }
  const logout = async () => { /* 登出 */ }
  const updateUserInfo = (newUserInfo) => { /* 更新用户信息 */ }
  const init = async () => { /* 初始化（页面刷新时调用） */ }

  return {
    token, user, isLoading, isInitialized, captchaId, captchaImage,
    isLoggedIn, isAdmin, username, nickname, avatar,
    fetchCaptcha, login, fetchCurrentUser, logout, updateUserInfo, init
  }
})
```

#### 3.2.2 设备状态管理 (`src/stores/deviceStore.js`)

设备状态管理包含：
- 设备列表数据
- 分页信息
- 搜索表单
- 设备操作（增删改查、批量操作）

### 3.3 API服务层 (Axios)

#### 3.3.1 Axios配置 (`src/api/index.js`)

```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'

export const resolveApiBaseUrl = (env = import.meta.env) => 
  env?.VITE_API_BASE_URL || '/api/v1'

const api = axios.create({
  baseURL: resolveApiBaseUrl(),
  timeout: 60000,
  headers: { 'Content-Type': 'application/json' }
})

// 请求拦截器 - 自动添加 Authorization Header
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器 - 统一错误处理
api.interceptors.response.use(
  response => {
    if (response.config.responseType === 'blob') {
      return response
    }
    return response.data
  },
  error => {
    // 统一错误处理：401、403、404、422、500等
    return Promise.reject(error)
  }
)
```

#### 3.3.2 API模块列表

| 模块 | 说明 | 主要方法 |
|------|------|----------|
| deviceApi | 设备管理 | getDevices, createDevice, updateDevice, deleteDevice, testConnectivity, executeCommand, batchExecuteCommand |
| portApi | 端口管理 | getPorts, createPort, updatePort, deletePort |
| vlanApi | VLAN管理 | getVlans, createVlan, updateVlan, deleteVlan |
| inspectionApi | 巡检管理 | getInspections, runInspection, batchRunInspections |
| configurationApi | 配置管理 | getConfigurations, collectConfigFromDevice, getConfigDiff, createBackupSchedule, backupNow, backupAll |
| gitConfigApi | Git配置 | getGitConfigs, testGitConnection, setActiveGitConfig |
| monitoringApi | 备份监控 | getStatistics, getDashboard, getExecutionLogs, getTrends |
| deviceCollectionApi | 设备采集 | collectDeviceVersion, collectDeviceSerial, collectInterfacesInfo, collectMacTable |
| authApi | 认证相关 | login, logout, getCaptcha, getCurrentUser, changePassword |
| userApi | 用户管理 | getUsers, createUser, updateUser, deleteUser, resetPassword |

### 3.4 根组件 (App.vue)

**布局结构**:

```vue
<template>
  <div class="app-container">
    <!-- 已登录时显示完整布局 -->
    <el-container v-if="isLoggedIn">
      <!-- 顶部导航栏 -->
      <el-header height="60px" class="header">
        <div class="logo">
          <h1>交换机管理系统</h1>
        </div>
        <div class="header-right">
          <el-dropdown>
            <span class="user-info">
              <el-avatar :size="32" :icon="UserIcon" />
              <span class="username">{{ userNickname }}</span>
            </span>
            <template #dropdown>
              <el-dropdown-menu>
                <el-dropdown-item @click="goToProfile">个人中心</el-dropdown-item>
                <el-dropdown-item divided @click="handleLogout">退出登录</el-dropdown-item>
              </el-dropdown-menu>
            </template>
          </el-dropdown>
        </div>
      </el-header>

      <!-- 主体内容 -->
      <el-container>
        <!-- 侧边栏 -->
        <el-aside width="200px" class="sidebar">
          <el-menu :default-active="activeIndex" router>
            <el-menu-item index="/">首页</el-menu-item>
            <el-menu-item index="/devices">设备管理</el-menu-item>
            <el-menu-item index="/ports">端口管理</el-menu-item>
            <el-menu-item index="/vlans">VLAN管理</el-menu-item>
            <el-menu-item index="/inspections">巡检管理</el-menu-item>
            <el-menu-item index="/configurations">配置管理</el-menu-item>
            <el-menu-item index="/backup-schedules">备份计划</el-menu-item>
            <el-menu-item index="/monitoring">备份监控</el-menu-item>
            <el-menu-item index="/device-collection">设备采集</el-menu-item>
            <el-menu-item index="/git-configs">Git配置管理</el-menu-item>
            <el-menu-item v-if="isAdmin" index="/users">用户管理</el-menu-item>
          </el-menu>
        </el-aside>

        <!-- 内容区域 -->
        <el-main class="main-content">
          <router-view></router-view>
        </el-main>
      </el-container>
    </el-container>

    <!-- 未登录时只显示路由内容（登录页） -->
    <template v-else>
      <router-view></router-view>
    </template>
  </div>
</template>
```

---

## 4. 页面组件设计

### 4.1 页面组件列表

| 页面 | 文件 | 主要功能 | 权限 |
|------|------|----------|------|
| 登录 | LoginView.vue | 验证码登录、记住我 | 公开 |
| 首页 | HomeView.vue | 系统概览 | 用户 |
| 设备管理 | DeviceManagement.vue | 设备CRUD、批量操作、命令执行、模板管理 | 用户 |
| 端口管理 | PortManagement.vue | 端口CRUD | 用户 |
| VLAN管理 | VLANManagement.vue | VLAN CRUD | 用户 |
| 巡检管理 | InspectionManagement.vue | 巡检执行、结果查看 | 用户 |
| 配置管理 | ConfigurationManagement.vue | 配置采集、版本管理 | 用户 |
| 备份计划 | BackupScheduleManagement.vue | 备份计划CRUD、立即备份 | 用户 |
| 备份监控 | BackupMonitoring.vue | 统计概览、趋势图表、执行日志 | 用户 |
| 设备采集 | DeviceCollection.vue | 设备信息采集 | 用户 |
| Git配置管理 | GitConfigManagement.vue | Git仓库配置 | 用户 |
| 用户管理 | UserManagement.vue | 用户CRUD、重置密码 | 管理员 |
| 个人中心 | ProfileView.vue | 个人信息修改、密码修改 | 用户 |

### 4.2 组件设计模式

**列表页面通用结构**:

```vue
<template>
  <div class="page-container">
    <!-- 页面标题 -->
    <el-card shadow="hover">
      <template #header>
        <div class="card-header">
          <span>页面标题</span>
          <div class="header-buttons">
            <el-button type="primary" @click="handleAdd">新增</el-button>
          </div>
        </div>
      </template>

      <!-- 搜索栏 -->
      <el-form :inline="true" :model="searchForm" class="search-form">
        <el-form-item label="状态">
          <el-select v-model="searchForm.status" clearable />
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="handleSearch">搜索</el-button>
          <el-button @click="handleReset">重置</el-button>
        </el-form-item>
      </el-form>

      <!-- 数据表格 -->
      <el-table :data="tableData" v-loading="loading" @selection-change="handleSelectionChange">
        <el-table-column type="selection" width="55" />
        <el-table-column prop="name" label="名称" />
        <el-table-column prop="status" label="状态">
          <template #default="{ row }">
            <el-tag :type="getStatusType(row.status)">{{ row.status }}</el-tag>
          </template>
        </el-table-column>
        <el-table-column label="操作" fixed="right">
          <template #default="{ row }">
            <el-button size="small" @click="handleEdit(row)">编辑</el-button>
            <el-button size="small" type="danger" @click="handleDelete(row)">删除</el-button>
          </template>
        </el-table-column>
      </el-table>

      <!-- 分页 -->
      <div class="pagination">
        <el-pagination
          v-model:current-page="currentPage"
          v-model:page-size="pageSize"
          :total="total"
          layout="total, sizes, prev, pager, next, jumper"
          @size-change="handleSizeChange"
          @current-change="handleCurrentChange"
        />
      </div>
    </el-card>

    <!-- 表单对话框 -->
    <el-dialog v-model="dialogVisible" :title="dialogTitle">
      <el-form :model="form" :rules="formRules" ref="formRef">
        <!-- 表单项 -->
      </el-form>
      <template #footer>
        <el-button @click="dialogVisible = false">取消</el-button>
        <el-button type="primary" @click="handleSubmit">确定</el-button>
      </template>
    </el-dialog>
  </div>
</template>
```

### 4.3 特色功能组件

#### 4.3.1 设备管理页面

设备管理页面包含丰富的功能：
- **搜索筛选**: 按状态、厂商筛选
- **批量操作**: 批量删除、批量更新状态、批量执行命令
- **设备上传**: Excel批量导入设备
- **命令执行**: 支持命令模板、变量替换、历史命令
- **模板管理**: 命令模板的CRUD

#### 4.3.2 备份监控页面

备份监控页面使用ECharts展示：
- **统计卡片**: 设备总数、备份计划数、成功率、今日备份数
- **趋势图表**: 最近N天的备份成功/失败趋势
- **状态分布**: 饼图展示执行状态分布
- **执行日志**: 最近的备份执行记录

---

## 5. 构建配置

### 5.1 Vite配置 (`vite.config.js`)

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '/api/v1')
      }
    }
  },
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: true,
    rollupOptions: {
      output: {
        manualChunks: {
          'element-plus': ['element-plus'],
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'echarts': ['echarts']
        }
      }
    }
  }
})
```

### 5.2 测试配置 (`vitest.config.js`)

```javascript
import { defineConfig } from 'vitest/config'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./test/setup.js']
  }
})
```

---

## 6. 认证与权限

### 6.1 认证流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              认证流程                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 用户访问页面                                                            │
│       │                                                                     │
│       ▼                                                                     │
│  2. 路由守卫检查                                                            │
│       │                                                                     │
│       ├── 公开页面 → 直接访问                                               │
│       │                                                                     │
│       └── 需认证页面                                                        │
│              │                                                              │
│              ├── 无Token → 跳转登录页                                       │
│              │                                                              │
│              └── 有Token                                                    │
│                     │                                                       │
│                     ├── 未初始化 → 调用 authStore.init()                    │
│                     │                                                       │
│                     └── 检查权限                                            │
│                            │                                                │
│                            ├── 权限不足 → 跳转首页 + 提示                    │
│                            │                                                │
│                            └── 权限足够 → 访问页面                           │
│                                                                             │
│  3. 登录流程                                                                │
│       │                                                                     │
│       ├── 获取验证码                                                        │
│       │                                                                     │
│       ├── 提交登录信息 (用户名、密码、验证码)                                │
│       │                                                                     │
│       ├── 成功 → 保存Token、用户信息 → 跳转目标页面                         │
│       │                                                                     │
│       └── 失败 → 刷新验证码 → 显示错误                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 权限控制

| 权限级别 | 说明 | 可访问页面 |
|----------|------|------------|
| 公开 | 无需登录 | 登录页 |
| 用户 | 普通登录用户 | 首页、设备管理、端口管理、VLAN管理、巡检管理、配置管理、备份计划、备份监控、设备采集、Git配置、个人中心 |
| 管理员 | 管理员角色 | 所有页面 + 用户管理 |

---

## 7. 前端性能优化

### 7.1 代码分割
- 路由懒加载
- 组件异步加载
- 第三方库分离 (element-plus, vue-vendor, echarts)

### 7.2 状态管理优化
- 只存储必要的状态
- 使用计算属性派生状态
- 避免深层嵌套的对象

### 7.3 网络请求优化
- 请求合并
- Token自动注入
- 统一错误处理
- Blob响应特殊处理

---

## 8. 总结

本项目前端架构具有以下特点：

1. **现代化技术栈**：Vue 3 + Composition API + Pinia
2. **完整的认证系统**：验证码登录、Token管理、权限控制
3. **模块化设计**：清晰的目录结构和模块划分
4. **状态管理**：Pinia提供简洁高效的状态管理
5. **组件化开发**：可复用的组件设计
6. **性能优化**：代码分割、懒加载等优化策略
7. **开发体验**：Vite提供极速的开发体验
8. **数据可视化**：ECharts图表展示监控数据

整体架构设计合理，易于维护和扩展。
