# switch_manage 生产环境数据库配置
# 场景：生产环境 (Scene 3)
# 风险等级：🔴 高

# ⚠️ 警告：生产环境操作需要确认！

DB_HOST=10.23.65.95
DB_PORT=3306
DB_NAME=switch_manage
DB_USER=root
DB_PASSWORD=[OylKbYLJf*Hx((4dEIf]
DB_URL=mysql+pymysql://root:[OylKbYLJf*Hx((4dEIf]@10.23.65.95:3306/switch_manage

# 应用配置
APP_ENV=production
DEBUG=False
APP_URL=http://10.23.65.95

# 说明
# - 生产环境服务器
# - 连接本地 MariaDB 容器（生产数据库）
# - ⚠️ 任何操作都需要确认
# - 🚫 禁止直接修改数据
# - 📝 所有操作需要记录日志

# 安全规则
# - 查询操作：一级确认（口头）
# - 配置修改：二级确认（口头 + 验证码）
# - 数据修改：三级确认（口头 + 验证码 + 飞书审批）
# - 部署/重启：三级确认 + 回滚方案
